# Core dependencies (installed by default)
numpy>=1.24.0
faiss-cpu>=1.7.4

# Optional dependencies (install as needed):
# GPU support: pip install faiss-gpu>=1.7.4
# Anthropic Claude: pip install anthropic>=0.39.0
# Document conversion: pip install pdfplumber>=0.10.0 pdf2image>=1.16.0 pytesseract>=0.3.10 python-bidi>=0.4.2 Pillow>=10.0.0 python-docx>=1.0.0

# Or use pyproject.toml extras:
# pip install -e ".[gpu]"       - GPU acceleration
# pip install -e ".[anthropic]" - Claude support
# pip install -e ".[converter]" - PDF/DOCX conversion
# pip install -e ".[all]"       - Everything
